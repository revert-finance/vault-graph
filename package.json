{
  "name": "revert-vault-polygon",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build --network arbitrum-one",
    "deploy": "graph deploy --network arbitrum-one --studio revert-vault-arbitrum"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.63.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
